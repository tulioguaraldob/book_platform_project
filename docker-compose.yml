version: "3.9"

services:
  db:
    image: mysql:latest
    container_name: db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: books

  cache:
    image: redis:latest
    container_name: cache
    restart: always