version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: db
    restart: always
    environment:
      - POSTGRES_PASSWORD=root
      - POSTGRES_DB=bank

    ports:
      - 5432:5432
